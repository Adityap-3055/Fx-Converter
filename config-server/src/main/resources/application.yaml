server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          # user.dir dynamically points to config-repo
          uri: file://${user.dir}/config-repo